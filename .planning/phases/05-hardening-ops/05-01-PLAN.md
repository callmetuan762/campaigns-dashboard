---
phase: 05-hardening-ops
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/config.py
  - src/main.py
  - src/meta/ingest.py
  - src/ga4/ingest.py
  - src/reports/daily.py
  - src/reports/weekly.py
  - src/alerts/engine.py
  - tests/test_sentry.py
autonomous: true
requirements: [SC-3]
tags: [sentry, observability, error-capture]

must_haves:
  truths:
    - "sentry_sdk.init() is called inside async main() when SENTRY_DSN is set, with AsyncioIntegration, traces_sample_rate=0.0, send_default_pii=False"
    - "sentry_sdk.init() is NOT called when SENTRY_DSN is absent — optional integration pattern"
    - "capture_exception(exc) is called at every catch-and-suppress except block in ingest and report modules"
    - "SENTRY_DSN is stored as SecretStr; .get_secret_value() is used when passing to init"
    - "sentry_environment defaults to 'production' and is overrideable via env var"
    - "All existing tests pass with sentry-sdk installed (no import side-effects)"
  artifacts:
    - path: pyproject.toml
      provides: "sentry-sdk>=2.60.0,<3 dependency"
      contains: "sentry-sdk"
    - path: src/config.py
      provides: "sentry_dsn and sentry_environment Settings fields"
      contains: "sentry_dsn"
    - path: src/main.py
      provides: "Conditional sentry_sdk.init() inside async main()"
      contains: "sentry_sdk.init"
    - path: tests/test_sentry.py
      provides: "Unit tests verifying init called/not-called and capture_exception called on failure"
      contains: "test_sentry_init_called_when_dsn_set"
  key_links:
    - from: src/config.py
      to: src/main.py
      via: "settings.sentry_dsn checked before calling sentry_sdk.init()"
      pattern: "if settings\\.sentry_dsn:"
    - from: src/main.py
      to: sentry_sdk
      via: "sentry_sdk.init(dsn=settings.sentry_dsn.get_secret_value())"
      pattern: "sentry_sdk\\.init"
    - from: src/meta/ingest.py
      to: sentry_sdk
      via: "sentry_sdk.capture_exception(exc) in outer except block"
      pattern: "capture_exception"

pitfalls:
  - "Pitfall 1: sentry_sdk.init() must be inside async main(), not at module level — AsyncioIntegration requires the event loop to exist"
  - "Pitfall 2: AsyncioIntegration only captures UNHANDLED exceptions; existing catch-and-suppress blocks need explicit capture_exception() calls at each site"
  - "Pitfall 6: capture_exception() is a safe no-op when SDK is uninitialized (sentry-sdk 2.x) — no per-call guard needed after the init guard"
---

<objective>
Add Sentry error capture to the ads-reporting agent: dependency declaration, Settings extension, conditional init inside async main(), and explicit capture_exception() calls at every catch-and-suppress site in ingest and report modules. Add a test suite that verifies the capture path works and the no-DSN path stays a no-op.

Purpose: Operators currently have no visibility into silently-caught exceptions. Sentry routes errors to a notification channel, groups duplicates, and provides stack-trace context — satisfying success criterion SC-3.

Output: 9 modified/created files; sentry-sdk installed; errors in ingest and report jobs captured automatically.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-hardening-ops/05-RESEARCH.md
@.planning/phases/05-hardening-ops/05-PATTERNS.md
@src/config.py
@src/main.py
@CLAUDE.md

<interfaces>
<!-- Existing patterns this plan extends. Extracted from codebase. -->

Settings (src/config.py) — optional SecretStr pattern already used:
```python
meta_app_secret: SecretStr | None = None
meta_access_token: SecretStr | None = None
anthropic_api_key: SecretStr | None = None
# Phase 5 ADD (after Anthropic block, before App config block):
sentry_dsn: SecretStr | None = None
sentry_environment: str = "production"
```

main.py boot sequence — optional integration guard pattern:
```python
async def main() -> None:
    settings = load_settings()
    # INSERT Sentry init here (after load_settings, before configure_logging)
    if settings.sentry_dsn:
        import sentry_sdk
        from sentry_sdk.integrations.asyncio import AsyncioIntegration
        sentry_sdk.init(
            dsn=settings.sentry_dsn.get_secret_value(),
            integrations=[AsyncioIntegration()],
            environment=settings.sentry_environment,
            traces_sample_rate=0.0,
            send_default_pii=False,
        )
    configure_logging(level=settings.log_level, fmt="json")
```

Catch-and-suppress pattern in ingest/report modules (add capture_exception as FIRST line):
```python
except Exception as exc:  # noqa: BLE001
    sentry_sdk.capture_exception(exc)   # NEW — add at every site listed below
    logger.error("...", error=str(exc))
```

Catch sites to update (outer except only — not inner guarded blocks):
  - src/meta/ingest.py: _run_meta_ingest outer except
  - src/ga4/ingest.py: _run_ga4_ingest outer except
  - src/reports/daily.py: _run_daily_report outer except
  - src/reports/weekly.py: _run_weekly_report outer except
  - src/alerts/engine.py: evaluate_alerts outer except

sentry_sdk is a no-op when not initialized (2.x behavior) — no guard needed at call sites.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dependency + Settings + sentry_sdk.init in main.py</name>
  <files>pyproject.toml, src/config.py, src/main.py</files>
  <read_first>
    - pyproject.toml (full — locate [project] dependencies list to append sentry-sdk)
    - src/config.py (full — find the Anthropic block to place Sentry fields after it; preserve field order)
    - src/main.py (full — find async main() to insert init call between load_settings() and configure_logging())
    - .planning/phases/05-hardening-ops/05-PATTERNS.md (sections "src/config.py" and "src/main.py")
  </read_first>
  <action>
    Edit pyproject.toml: in the `[project]` `dependencies` list, append `"sentry-sdk>=2.60.0,<3"` as the last dependency entry. Do not change any other dependency or configuration.

    Edit src/config.py: after the `# ---- Anthropic (Phase 4) ----` block and before the `# ---- App config ----` block, insert:
    ```python
    # ---- Sentry (Phase 5) ----
    sentry_dsn: SecretStr | None = None
    sentry_environment: str = "production"
    ```
    Do not add a validator. `SecretStr | None` parses from `SENTRY_DSN` env var automatically via pydantic-settings. Do not touch any other field or validator.

    Edit src/main.py: immediately after `settings = load_settings()` and before the `configure_logging(...)` call, insert the conditional Sentry init block:
    ```python
    if settings.sentry_dsn:
        import sentry_sdk
        from sentry_sdk.integrations.asyncio import AsyncioIntegration
        sentry_sdk.init(
            dsn=settings.sentry_dsn.get_secret_value(),
            integrations=[AsyncioIntegration()],
            environment=settings.sentry_environment,
            traces_sample_rate=0.0,
            send_default_pii=False,
        )
    ```
    Placement is critical: INSIDE `async def main()`, AFTER `load_settings()`, BEFORE `configure_logging()`. Do not call `sentry_sdk.init()` at module level. Do not import sentry_sdk at the top of main.py — the lazy import inside the guard is intentional (avoids importing when DSN is absent).
  </action>
  <verify>
    <automated>python -c "from src.config import Settings; s=Settings(telegram_bot_token='x'); assert s.sentry_dsn is None; assert s.sentry_environment == 'production'; print('OK defaults')"</automated>
    <automated>python -c "from src.config import Settings; from pydantic import SecretStr; s=Settings(telegram_bot_token='x', sentry_dsn='https://fake@sentry.io/1', sentry_environment='staging'); assert s.sentry_dsn.get_secret_value() == 'https://fake@sentry.io/1'; assert s.sentry_environment == 'staging'; print('OK override')"</automated>
    <automated>python -c "import ast, pathlib; src=pathlib.Path('src/main.py').read_text(); assert 'sentry_sdk.init' in src; assert 'if settings.sentry_dsn:' in src; assert 'AsyncioIntegration' in src; assert 'send_default_pii=False' in src; print('OK main.py')"</automated>
    <automated>python -c "import pathlib; t=pathlib.Path('pyproject.toml').read_text(); assert 'sentry-sdk' in t; print('OK pyproject')"</automated>
  </verify>
  <done>
    pyproject.toml declares sentry-sdk>=2.60.0,<3; Settings has sentry_dsn (SecretStr|None) and sentry_environment (str, default "production"); main.py conditionally inits sentry_sdk inside async main() with AsyncioIntegration, traces_sample_rate=0.0, send_default_pii=False.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add capture_exception() to all catch-and-suppress sites</name>
  <files>src/meta/ingest.py, src/ga4/ingest.py, src/reports/daily.py, src/reports/weekly.py, src/alerts/engine.py</files>
  <read_first>
    - src/meta/ingest.py (full — find _run_meta_ingest outer except block)
    - src/ga4/ingest.py (full — find _run_ga4_ingest outer except block)
    - src/reports/daily.py (full — find _run_daily_report outer except block)
    - src/reports/weekly.py (full — find _run_weekly_report outer except block)
    - src/alerts/engine.py (full — find evaluate_alerts outer except block)
    - .planning/phases/05-hardening-ops/05-RESEARCH.md (Pitfalls 1 and 2, Topic 3 "Explicit Capture at Catch Sites")
  </read_first>
  <action>
    In EACH of the five files, add `import sentry_sdk` at the top of the file (after existing imports, before the logger declaration) and add `sentry_sdk.capture_exception(exc)` as the FIRST line inside the outer `except Exception as exc:` block that catches and suppresses errors (i.e., logs but does not re-raise).

    The pattern at each site must become:
    ```python
    except Exception as exc:  # noqa: BLE001
        sentry_sdk.capture_exception(exc)
        logger.error("...", error=str(exc))
        # ... existing handling continues unchanged ...
    ```

    Specific locations to find in each file:
    - src/meta/ingest.py: the outer `except Exception as exc:` in `_run_meta_ingest` (the one that logs `"meta_ingest_failed"` or equivalent)
    - src/ga4/ingest.py: the outer `except Exception as exc:` in `_run_ga4_ingest`
    - src/reports/daily.py: the outer `except Exception as exc:` in `_run_daily_report` (the one that logs `"daily_report_failed"`)
    - src/reports/weekly.py: the outer `except Exception as exc:` in `_run_weekly_report` (the one that logs `"weekly_report_failed"`)
    - src/alerts/engine.py: the outer `except Exception as exc:` in `evaluate_alerts`

    Rules:
    - `sentry_sdk.capture_exception(exc)` is safe to call even when sentry_sdk.init() was not called (2.x no-op behavior) — do NOT add `if settings.sentry_dsn:` guards at each call site.
    - Do not add capture_exception to inner try/except blocks (e.g., per-campaign loops) — only the outermost catch-and-suppress block per function.
    - Do not change any other logic, SQL, or error handling in these files.
    - Preserve the `# noqa: BLE001` comment on each broad except clause.
  </action>
  <verify>
    <automated>python -c "import pathlib; files=['src/meta/ingest.py','src/ga4/ingest.py','src/reports/daily.py','src/reports/weekly.py','src/alerts/engine.py']; [print(f) or (_ for _ in [None] if 'sentry_sdk.capture_exception' not in pathlib.Path(f).read_text()).__next__() if 'sentry_sdk.capture_exception' not in pathlib.Path(f).read_text() else None for f in files]; print('All 5 files have capture_exception')" 2>&1 || python -c "import pathlib; files=['src/meta/ingest.py','src/ga4/ingest.py','src/reports/daily.py','src/reports/weekly.py','src/alerts/engine.py']; missing=[f for f in files if 'sentry_sdk.capture_exception' not in pathlib.Path(f).read_text()]; assert not missing, f'Missing capture_exception in: {missing}'; print('OK all 5 files')"</automated>
    <automated>python -c "import pathlib; files=['src/meta/ingest.py','src/ga4/ingest.py','src/reports/daily.py','src/reports/weekly.py','src/alerts/engine.py']; missing=[f for f in files if 'import sentry_sdk' not in pathlib.Path(f).read_text()]; assert not missing, f'Missing import in: {missing}'; print('OK all imports')"</automated>
    <automated>pytest tests/ --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    All 5 catch-and-suppress sites have `import sentry_sdk` at module level and `sentry_sdk.capture_exception(exc)` as the first line of their outer except block. No other logic changed. pytest collection passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: tests/test_sentry.py — verify init and capture paths</name>
  <files>tests/test_sentry.py</files>
  <read_first>
    - tests/test_heartbeat.py (analog — conditional integration, patch + assert_called pattern)
    - .planning/phases/05-hardening-ops/05-PATTERNS.md (section "tests/test_sentry.py")
    - src/main.py (to know the exact import path to patch)
  </read_first>
  <behavior>
    - test_sentry_init_called_when_dsn_set: patch sentry_sdk.init; create Settings with sentry_dsn set; call main() entrypoint or the init block; assert sentry_sdk.init called once with correct dsn string and send_default_pii=False.
    - test_sentry_init_not_called_without_dsn: patch sentry_sdk.init; create Settings without sentry_dsn; assert sentry_sdk.init NOT called.
    - test_capture_exception_called_on_meta_ingest_failure: patch _run_meta_ingest's outer dependency (e.g., db.fetch_all) to raise; patch sentry_sdk.capture_exception; call _run_meta_ingest; assert capture_exception was called once.
    - test_no_raise_when_capture_exception_called_uninitialized: call sentry_sdk.capture_exception(Exception("test")) WITHOUT calling sentry_sdk.init() first; assert no exception is raised.
  </behavior>
  <action>
    Create tests/test_sentry.py following the test file conventions from 05-PATTERNS.md:

    ```python
    """Prove SC-3: Sentry captures exceptions at ingest failure sites; no-op without DSN."""
    from __future__ import annotations

    from unittest.mock import AsyncMock, MagicMock, patch

    import pytest

    pytestmark = pytest.mark.asyncio
    ```

    Test 1 — init called with DSN: patch `sentry_sdk.init` (use `patch("sentry_sdk.init")`), construct a Settings instance with `sentry_dsn="https://fake@sentry.io/123"`, invoke the conditional init block (import and call the init inline or use a helper), assert `mock_init.assert_called_once()` and that the `dsn` kwarg equals the secret value string (not a SecretStr object), and that `send_default_pii=False` was passed.

    Test 2 — init not called without DSN: same patch, construct Settings with `sentry_dsn=None`, assert `mock_init.assert_not_called()`.

    Test 3 — capture_exception called on ingest failure: patch `sentry_sdk.capture_exception`; import `_run_meta_ingest` from `src.meta.ingest`; pass a `db` mock whose `fetch_all` is an `AsyncMock(side_effect=RuntimeError("db down"))`; call `await _run_meta_ingest(bot=None, db=mock_db, settings=mock_settings)`; assert `mock_capture.assert_called_once()` and the argument is an instance of `RuntimeError`.

    Test 4 — no-op without init: call `sentry_sdk.capture_exception(Exception("test"))` directly (no init call in this test); assert no exception propagates. This is a smoke test for the assumption in Pitfall 6.

    Follow the module-level logger and pytestmark patterns from test_heartbeat.py. All tests are pure unit tests — no db_client fixture needed.
  </action>
  <verify>
    <automated>pytest tests/test_sentry.py -x -q</automated>
  </verify>
  <done>
    tests/test_sentry.py has 4 passing tests covering init/no-init branching, capture_exception at a representative catch site, and no-op behavior when SDK is uninitialized.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SENTRY_DSN env var → sentry_sdk.init | DSN is treated as a secret; extracted via SecretStr.get_secret_value() — never logged |
| Exception data → Sentry | Exception messages and stack traces leave the process boundary; send_default_pii=False prevents PII leakage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Information Disclosure | SENTRY_DSN in Settings | mitigate | `sentry_dsn: SecretStr` — structlog redaction pipeline already masks SecretStr fields (INFRA-05); `.get_secret_value()` called only at init site |
| T-05-01-02 | Information Disclosure | Exception payloads sent to Sentry | mitigate | `send_default_pii=False` in sentry_sdk.init(); campaign names and ad copy in exceptions are already wrapped in data-tag strings (CLAUDE.md requirement) |
| T-05-01-03 | Tampering | sentry_sdk.init called with injected DSN | accept | DSN is operator-supplied via env var; no user input path to this field |
| T-05-01-04 | Denial of Service | Sentry SDK network calls blocking async loop | mitigate | AsyncioIntegration uses async-safe transport; traces_sample_rate=0.0 disables performance tracing overhead |
</threat_model>

<verification>
- `python -c "from src.config import Settings; s=Settings(telegram_bot_token='x'); assert s.sentry_dsn is None"` passes (no-DSN default).
- `python -c "import pathlib; t=pathlib.Path('pyproject.toml').read_text(); assert 'sentry-sdk' in t"` confirms dependency declared.
- `pytest tests/test_sentry.py -x -q` — all 4 tests pass.
- `pytest tests/ -x -q` — full suite passes with no regression from sentry-sdk import.
</verification>

<success_criteria>
- pyproject.toml: sentry-sdk>=2.60.0,<3 in [project] dependencies
- src/config.py: sentry_dsn (SecretStr|None) and sentry_environment (str="production") fields present
- src/main.py: conditional sentry_sdk.init() block inside async main() with AsyncioIntegration, traces_sample_rate=0.0, send_default_pii=False
- All 5 catch-and-suppress sites have sentry_sdk.capture_exception(exc) as first line of outer except
- tests/test_sentry.py passes (4 tests)
- pytest tests/ -x -q passes (no regression)
</success_criteria>

<output>
After completion, create `.planning/phases/05-hardening-ops/05-01-SUMMARY.md` per the GSD summary template.
</output>
