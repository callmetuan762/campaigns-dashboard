---
phase: 04-conversational-ai-recommendations
plan: 05
type: execute
wave: 3
depends_on: ["04-03", "04-04"]
files_modified:
  - src/bot/setup.py
  - src/reports/daily.py
  - src/reports/weekly.py
files_modified_list: [src/bot/setup.py, src/reports/daily.py, src/reports/weekly.py]
autonomous: true
requirements: [CHAT-01, CHAT-06, CHAT-07]
tags: [wiring, setup, scheduler, integration]

must_haves:
  truths:
    - "src/bot/setup.py wires Settings into the dispatcher workflow_data so chat_router handlers can resolve settings: Settings"
    - "src/bot/setup.py includes build_chat_router() AFTER build_router() — command router is FIRST (Pitfall 4)"
    - "src/reports/daily.py calls generate_tldr with db= so TL;DR usage is logged"
    - "src/reports/weekly.py calls generate_tldr with db= so weekly TL;DR usage is logged"
    - "No other behavior changes in daily.py / weekly.py — heartbeat, chart, splitter wiring untouched"
  artifacts:
    - path: src/bot/setup.py
      provides: "Chat router included after command router; settings injected into dp"
      contains: "build_chat_router()"
    - path: src/reports/daily.py
      provides: "generate_tldr call updated to pass db"
      contains: "db=db"
    - path: src/reports/weekly.py
      provides: "generate_tldr call updated to pass db"
      contains: "db=db"
  key_links:
    - from: src/bot/setup.py
      to: src/bot/chat_router.py (build_chat_router)
      via: "dp.include_router(build_chat_router()) AFTER include_router(build_router())"
      pattern: "include_router.*build_chat_router"
    - from: src/bot/setup.py
      to: chat_router handlers
      via: 'dp["settings"] = settings so handlers receive settings: Settings parameter'
      pattern: 'dp\["settings"\]'
    - from: src/reports/daily.py and weekly.py
      to: anthropic_usage_log via generate_tldr(db=db)
      via: "generate_tldr now logs usage row when db is provided"
      pattern: "db=db"
---

<objective>
Wire Phase 4 into the running app: register `build_chat_router()` in setup.py AFTER the command router (Pitfall 4), inject `settings` into the dispatcher workflow_data so chat_router handlers receive it as a parameter, and plumb `db` through to `generate_tldr` calls in daily/weekly reports so scheduled TL;DR API spend is counted against the monthly budget ceiling (Pitfall 8).

Purpose: Without this plan, chat_router.py exists but never receives messages, and TL;DR API calls bypass the budget gate. This is the integration step that makes Phase 4 actually live in the running bot.

Output: 3 modified files, behavior strictly additive (no removed functionality).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-conversational-ai-recommendations/04-CONTEXT.md
@.planning/phases/04-conversational-ai-recommendations/04-RESEARCH.md
@.planning/phases/04-conversational-ai-recommendations/04-PATTERNS.md
@.planning/phases/04-conversational-ai-recommendations/04-03-PLAN.md
@.planning/phases/04-conversational-ai-recommendations/04-04-PLAN.md
@src/bot/setup.py
@src/reports/daily.py
@src/reports/weekly.py
@CLAUDE.md

<interfaces>
Current setup.py shape (Phase 1-3):

    def create_bot_and_dispatcher(settings, db_client) -> tuple[Bot, Dispatcher]:
        bot = Bot(...)
        dp = Dispatcher()
        dp["db"] = db_client                              # Phase 1 injection
        allowlist = AllowlistMiddleware(...)
        dp.message.middleware(allowlist)
        dp.callback_query.middleware(allowlist)
        dp.include_router(build_router())                 # command router
        return bot, dp

Phase 4 additions:
    dp["settings"] = settings                              # NEW — chat_router needs it
    dp.include_router(build_chat_router())                 # NEW — AFTER build_router()

generate_tldr signature (after plan 04-03 Task 2):

    async def generate_tldr(
        api_key: str,
        campaign_rows: list[dict],
        date: str,
        db: DBClient | None = None,
    ) -> str | None

Current call sites:
- src/reports/daily.py line ~163:
    tldr = await generate_tldr(api_key, yesterday_rows, yesterday)
- src/reports/weekly.py line ~113:
    tldr = await generate_tldr(api_key, this_week_rows, f"week ending {week_end}")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire chat_router + settings injection in src/bot/setup.py</name>
  <files>src/bot/setup.py</files>
  <read_first>
    - src/bot/setup.py (full — current ~60 lines; append only, do not remove existing lines)
    - src/bot/chat_router.py (signature of build_chat_router and required dp injections)
    - .planning/phases/04-conversational-ai-recommendations/04-RESEARCH.md (lines 561-569 Pitfall 4)
    - .planning/phases/04-conversational-ai-recommendations/04-PATTERNS.md ("src/bot/setup.py" section)
  </read_first>
  <action>
    Edit `src/bot/setup.py`:

    1. Add a new import line directly below `from src.bot.handlers import build_router`:

           from src.bot.chat_router import build_chat_router

    2. After the existing line `dp["db"] = db_client`, add the settings injection. Insert this as a single new line at the same indentation level:

           dp["settings"] = settings

       And add a one-line comment above it explaining the purpose:

           # Phase 4: chat_router handlers declare `settings: Settings` as a parameter.

    3. Replace the existing single line `dp.include_router(build_router())` with the two-line ordered block (command router FIRST, catch-all SECOND — Pitfall 4 mitigation). Use this exact replacement (4-space indented inside the function):

           # STEP 2: Include routers in priority order — command router FIRST,
           # catch-all SECOND. If chat_router were included before build_router(),
           # the F.text catch-all would intercept /commands before the Command()
           # filters see them (RESEARCH Pitfall 4).
           dp.include_router(build_router())          # commands: /start /status /help /report /clear
           dp.include_router(build_chat_router())     # catch-all: non-command text + inline buttons (Phase 4)

    4. Update the trailing `logger.info("bot_dispatcher_ready", ...)` call to include a `phase=4` key. Existing kwargs MUST remain — only the `phase` key is added:

           logger.info(
               "bot_dispatcher_ready",
               phase=4,
               allowed_chats=len(settings.telegram_allowed_chat_ids),
               allowed_users=len(settings.telegram_allowed_user_ids),
           )

    Rules:
    - Do NOT remove or reorder any existing line (allowlist registration, middleware on message + callback_query).
    - The chat_router include MUST come strictly AFTER the command router include — same indentation, the immediately following statement.
    - `dp["settings"] = settings` must appear BEFORE both include_router calls (handler injections are read at handler invocation, but placing them up-front is the convention used for `dp["db"]` in this codebase).
  </action>
  <verify>
    <automated>python -c "from src.bot.setup import create_bot_and_dispatcher; import inspect; src = inspect.getsource(create_bot_and_dispatcher); assert 'build_chat_router' in src; assert 'dp[\"settings\"]' in src; idx_cmd = src.find('include_router(build_router())'); idx_chat = src.find('include_router(build_chat_router())'); assert idx_cmd != -1 and idx_chat != -1 and idx_cmd < idx_chat, (idx_cmd, idx_chat); print('OK')"</automated>
    <automated>python -c "from src.bot import setup; assert 'from src.bot.chat_router import build_chat_router' in open(setup.__file__).read(); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep finds `from src.bot.chat_router import build_chat_router` in src/bot/setup.py.
    - grep finds `dp["settings"] = settings` in src/bot/setup.py.
    - grep finds BOTH `dp.include_router(build_router())` AND `dp.include_router(build_chat_router())` in src/bot/setup.py.
    - The line offset of `include_router(build_router())` is strictly less than `include_router(build_chat_router())` (Pitfall 4 ordering — verified by the inline assert in <verify>).
    - The middleware registration lines (`dp.message.middleware(allowlist)` and `dp.callback_query.middleware(allowlist)`) are still present and unchanged.
    - grep finds `phase=4` inside the `logger.info("bot_dispatcher_ready", ...)` call.
    - `python -c "from src.bot.setup import create_bot_and_dispatcher"` succeeds.
  </acceptance_criteria>
  <done>
    setup.py imports build_chat_router, injects settings into the dispatcher, and registers the chat router AFTER the command router. Catch-all ordering is correct (Pitfall 4 mitigated by both code and inline test).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Pass db= to generate_tldr in daily.py and weekly.py</name>
  <files>src/reports/daily.py, src/reports/weekly.py</files>
  <read_first>
    - src/reports/daily.py (lines 155-170 — the existing generate_tldr call site)
    - src/reports/weekly.py (lines 105-120 — the existing generate_tldr call site)
    - .planning/phases/04-conversational-ai-recommendations/04-03-PLAN.md (Task 2 — new generate_tldr signature: api_key, campaign_rows, date, db=None)
    - .planning/phases/04-conversational-ai-recommendations/04-RESEARCH.md (Pitfall 8 — TL;DR calls must count against monthly budget)
  </read_first>
  <action>
    Edit `src/reports/daily.py`:

    Locate the existing line (around line 163):

        tldr = await generate_tldr(api_key, yesterday_rows, yesterday)

    Replace it with:

        tldr = await generate_tldr(api_key, yesterday_rows, yesterday, db=db)

    No other change in daily.py. Do NOT touch heartbeat ordering, chart generation, or splitter calls.

    Edit `src/reports/weekly.py`:

    Locate the existing line (around line 113):

        tldr = await generate_tldr(api_key, this_week_rows, f"week ending {week_end}")

    Replace it with:

        tldr = await generate_tldr(api_key, this_week_rows, f"week ending {week_end}", db=db)

    No other change in weekly.py.

    Rules:
    - The `db` parameter is passed by KEYWORD (`db=db`) to avoid positional confusion — generate_tldr's positional order is (api_key, campaign_rows, date), and `db` is the 4th positional/keyword parameter.
    - The `db` value at the call site is the local variable from `_run_daily_report(bot, db, settings)` / `_run_weekly_report(bot, db, settings)` — already in scope.
    - Do NOT add try/except around the generate_tldr call — the function already handles all internal errors and returns None on failure (Phase 2 invariant unchanged).
  </action>
  <verify>
    <automated>python -c "src1 = open('src/reports/daily.py').read(); src2 = open('src/reports/weekly.py').read(); assert 'generate_tldr(api_key, yesterday_rows, yesterday, db=db)' in src1, 'daily.py missing db=db'; assert 'db=db' in src2 and 'generate_tldr' in src2, 'weekly.py missing db=db'; print('OK')"</automated>
    <automated>python -c "from src.reports import daily, weekly; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep finds `generate_tldr(api_key, yesterday_rows, yesterday, db=db)` in src/reports/daily.py (exact substring).
    - grep finds the substring `db=db` in src/reports/weekly.py inside the same line as `generate_tldr(`.
    - grep does NOT find the old call `generate_tldr(api_key, yesterday_rows, yesterday)` (no trailing-paren-on-arg version) in daily.py.
    - grep does NOT find the old call `generate_tldr(api_key, this_week_rows, f"week ending {week_end}")` (without db=db) in weekly.py.
    - `python -c "from src.reports import daily, weekly"` succeeds.
    - Heartbeat ordering (`await ping_heartbeat(settings.heartbeat_url)` AFTER all `await bot.send_*` calls) is unchanged in both files.
  </acceptance_criteria>
  <done>
    Both scheduled report jobs now pass `db=db` to `generate_tldr`. TL;DR token usage will be recorded in anthropic_usage_log, so the monthly budget gate in chat.py reflects total Anthropic spend (Pitfall 8 mitigated).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dispatcher workflow_data → handlers | `dp["settings"]` and `dp["db"]` are injected at boot from the main.py-loaded Settings + DBClient. No untrusted input crosses this boundary. |
| generate_tldr → anthropic_usage_log | TL;DR token counts come from the SDK Usage object; cost via calculate_cost. Pricing uses the corrected Haiku 4.5 rate ($1.00/$5.00 — plan 04-02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Tampering | Router ordering | mitigate | Inline test (`idx_cmd < idx_chat`) plus comment block in setup.py enforce command-router-first ordering. If a future edit reverses the order, the test fails. |
| T-04-05-02 | DoS | Monthly budget bypass via TL;DR | mitigate | daily.py + weekly.py now pass db=db to generate_tldr. Pitfall 8 mitigated; total spend reflected in get_monthly_anthropic_cost(). |
| T-04-05-03 | Information Disclosure | Settings leaked via dp injection | accept | Settings object contains SecretStr fields (anthropic_api_key, telegram_bot_token, meta_access_token) — handlers receive the full Settings but only access non-secret attributes. SecretStr.get_secret_value() must be explicit at API call sites. Phase 1-3 already follow this convention. |
| T-04-05-04 | Repudiation | TL;DR cost untracked on db logging failure | mitigate | generate_tldr's try/except inside the if-db branch (plan 04-03 Task 2) logs `tldr_usage_log_failed` via structlog; failure is observable but does not crash the report. |
</threat_model>

<verification>
- chat_router is registered AFTER command router (verified by inline `idx_cmd < idx_chat` assertion in Task 1 verify).
- Settings is reachable as `settings: Settings` parameter in chat_router handlers (will be confirmed end-to-end in plan 04-06 tests).
- TL;DR call sites pass db=db (grep-verified in Task 2 acceptance_criteria).
</verification>

<success_criteria>
- 3 files modified.
- All <automated> verify commands pass.
- pytest --collect-only -q succeeds.
- No regression to Phase 1-3 wiring (allowlist, command router, scheduler).
- The bot can be imported and `create_bot_and_dispatcher` can be called against a real Settings + DBClient (smoke-tested in 04-06).
</success_criteria>

<output>
After completion, create `.planning/phases/04-conversational-ai-recommendations/04-05-SUMMARY.md`.
</output>
