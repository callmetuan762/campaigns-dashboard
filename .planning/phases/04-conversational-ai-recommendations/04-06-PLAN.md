---
phase: 04-conversational-ai-recommendations
plan: 06
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03", "04-04", "04-05"]
files_modified:
  - tests/test_ai_tools.py
  - tests/test_ai_chat.py
  - tests/test_chat_router.py
  - tests/test_phase4_handlers.py
files_modified_list: [tests/test_ai_tools.py, tests/test_ai_chat.py, tests/test_chat_router.py, tests/test_phase4_handlers.py]
autonomous: true
requirements: [CHAT-01, CHAT-02, CHAT-03, CHAT-04, CHAT-05, CHAT-06, CHAT-07, CHAT-08, REC-01, REC-02, REC-03]
tags: [tests, pytest, asyncio]

must_haves:
  truths:
    - "tests/test_ai_tools.py covers all 5 tool functions: success path with seeded data, invalid metric/source/sort_by returns error string, empty-table returns 'no data' string"
    - "tests/test_ai_tools.py asserts Haiku 4.5 pricing is $1.00/$5.00 per MTok (CORRECTED)"
    - "tests/test_ai_tools.py asserts every tool output contains a '(Source:' citation line"
    - "tests/test_ai_chat.py covers budget gate exhaustion, system prompt content, tool-use loop iteration cap, user-data wrapping, history persistence, and usage logging"
    - "tests/test_chat_router.py covers catch-all filter, inline keyboard shape, callback.answer() ordering, show_chart vs other-button routing"
    - "tests/test_phase4_handlers.py covers /clear command per-user scoping and updated /help text"
    - "All 11 Phase 4 requirement IDs (CHAT-01..08, REC-01..03) appear in test docstrings or pytest.mark for traceability"
    - "Existing Phase 1-3 test suite still passes (no regression)"
  artifacts:
    - path: tests/test_ai_tools.py
      provides: "tools.py unit + integration tests"
      contains: "test_query_metrics"
      min_lines: 200
    - path: tests/test_ai_chat.py
      provides: "chat.py orchestration tests with mocked Anthropic client"
      contains: "test_budget_exhausted"
      min_lines: 200
    - path: tests/test_chat_router.py
      provides: "Inline keyboard + CallbackQuery + catch-all router tests"
      contains: "test_build_followup_keyboard"
      min_lines: 100
    - path: tests/test_phase4_handlers.py
      provides: "/clear command + /help body tests"
      contains: "test_clear_scoped"
      min_lines: 50
  key_links:
    - from: tests/test_ai_chat.py
      to: src/ai/chat.py (handle_chat_message)
      via: "Async tests with mocked AsyncAnthropic responses"
      pattern: "handle_chat_message"
---

<objective>
Add 4 new test files covering every Phase 4 module with at least one assertion per requirement ID. Tests use the existing `db_client` pytest_asyncio fixture from conftest.py and the established `AsyncMock`/`MagicMock` pattern for bot + Anthropic stubs. Existing Phase 1-3 tests must continue to pass.

Purpose: This plan is the verification surface for the entire phase. Without it, the goal-backward must_haves in plans 01-05 are aspirational; with it, they are mechanically enforced.

Output: 4 new test files, all passing on the same `pytest -q` invocation that runs the existing 115-test Phase 3 suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-conversational-ai-recommendations/04-CONTEXT.md
@.planning/phases/04-conversational-ai-recommendations/04-RESEARCH.md
@.planning/phases/04-conversational-ai-recommendations/04-PATTERNS.md
@.planning/phases/04-conversational-ai-recommendations/04-01-PLAN.md
@.planning/phases/04-conversational-ai-recommendations/04-02-PLAN.md
@.planning/phases/04-conversational-ai-recommendations/04-03-PLAN.md
@.planning/phases/04-conversational-ai-recommendations/04-04-PLAN.md
@.planning/phases/04-conversational-ai-recommendations/04-05-PLAN.md
@tests/conftest.py
@tests/test_tldr.py
@tests/test_alert_engine.py
@tests/test_allowlist.py

<interfaces>
Existing test fixture (tests/conftest.py):

    @pytest_asyncio.fixture
    async def db_client(tmp_path: Path):
        client = DBClient(tmp_path / "test.db")
        await client.connect()
        await client.execute(
            "INSERT INTO campaigns (id, source, name, status) VALUES (?, ?, ?, ?)",
            ("c_1", "meta_ads", "Test Campaign", "ACTIVE"),
        )
        try:
            yield client
        finally:
            await client.close()

Existing test patterns used in tests/test_alert_engine.py and tests/test_tldr.py:
- `pytest.mark.asyncio` on async test functions
- `AsyncMock()` for awaitables; `MagicMock()` for sync attributes
- Seeding fact rows directly via `db_client.execute(...)` with raw INSERT and tuple params

Existing tests/test_tldr.py uses unittest.mock.patch to stub AsyncAnthropic; follow the same pattern for tests/test_ai_chat.py.

Phase 4 modules under test:
- src/ai/tools.py: TOOLS, dispatch_tool, calculate_cost, 5 async tool functions
- src/ai/chat.py: handle_chat_message, _SYSTEM_PROMPT, BUDGET_EXHAUSTED_USER_MSG
- src/bot/chat_router.py: build_chat_router, ChatAction, build_followup_keyboard
- src/bot/handlers.py: cmd_clear (within build_router)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create tests/test_ai_tools.py + tests/test_phase4_handlers.py</name>
  <files>tests/test_ai_tools.py, tests/test_phase4_handlers.py</files>
  <read_first>
    - tests/conftest.py (db_client fixture)
    - tests/test_alert_engine.py (lines 1-80 — pattern for seeding ad_metrics + named-param SQL test calls)
    - tests/test_tldr.py (pattern for mocking AsyncAnthropic)
    - src/ai/tools.py (final post-04-02 state — all 5 tool signatures and validation behavior)
    - src/bot/handlers.py (post-04-04 state — /clear + updated /help)
    - .planning/phases/04-conversational-ai-recommendations/04-02-PLAN.md (verify section — for expected error string prefixes)
    - .planning/phases/04-conversational-ai-recommendations/04-04-PLAN.md (Task 2 — /clear scoping behavior)
  </read_first>
  <behavior>
    test_ai_tools.py:
      - test_pricing_haiku45 — assert _PRICING['claude-haiku-4-5'] == (1.00, 5.00) (CORRECTED — Pitfall 5)
      - test_pricing_sonnet46 — assert _PRICING['claude-sonnet-4-6'] == (3.00, 15.00)
      - test_calculate_cost_sonnet — 1M input tokens at sonnet = $3.00
      - test_calculate_cost_unknown_model_falls_back — unknown model uses Sonnet rate
      - test_tools_list_shape — TOOLS has exactly 5 entries, names in expected order, each has input_schema + required
      - test_query_metrics_invalid_source — returns error string
      - test_query_metrics_meta_empty — returns "no data" string for empty DB
      - test_query_metrics_meta_seeded — seed ad_metrics, query returns spend and citation
      - test_query_metrics_ga4_seeded — seed ga4_metrics, query returns sessions and citation
      - test_compare_periods_invalid_metric — returns error string
      - test_compare_periods_seeded — covers both Meta and GA4 metric paths
      - test_get_campaign_detail_no_data — returns "No data for campaign" message
      - test_get_campaign_detail_with_data — joins meta + ga4, contains both source labels
      - test_list_underperformers_invalid_metric — error string
      - test_list_underperformers_seeded — finds underperformers below threshold
      - test_get_landing_page_performance_invalid_sort — error string
      - test_get_landing_page_performance_seeded — sorts by conversions or sessions
      - test_dispatch_tool_unknown — returns "Error: unknown tool"
      - test_dispatch_tool_routes_query_metrics — happy path through dispatch

    test_phase4_handlers.py:
      - test_clear_scoped — alice clears her history; bob's survives (per (chat_id, user_id) scoping)
      - test_help_text_contains_clear — /help body lists /clear and Phase 4 footer
  </behavior>
  <action>
    Create `tests/test_ai_tools.py` with all unit + integration tests for src/ai/tools.py. Use these conventions:
    - `import pytest`, `import pytest_asyncio`
    - All async tests marked with `@pytest.mark.asyncio`
    - Use the existing `db_client` fixture from conftest.py
    - Seed ad_metrics with raw INSERT statements using named parameters (mirror tests/test_alert_engine.py)
    - For each tool, write a parametrized invalid-input test plus 1-2 happy-path tests
    - Every requirement ID this file covers MUST appear in a docstring of at least one test (so `grep "CHAT-02" tests/test_ai_tools.py` returns hits)

    Example test stub for query_metrics (provided to anchor the others):

        @pytest.mark.asyncio
        async def test_query_metrics_invalid_source(db_client):
            """CHAT-02 D-13: invalid source returns error string, never raises."""
            from src.ai.tools import query_metrics
            result = await query_metrics(db_client, "bogus", "2026-05-01", "2026-05-02")
            assert result.startswith("Error: source"), result


        @pytest.mark.asyncio
        async def test_query_metrics_meta_seeded(db_client):
            """CHAT-02 / CHAT-04: returns aggregated row + (Source: ...) citation."""
            from src.ai.tools import query_metrics
            await db_client.execute(
                "INSERT INTO ad_metrics (campaign_id, date, ad_set_id, ad_id, spend, roas, "
                "meta_purchases_7dclick, clicks, impressions, fetched_at) VALUES "
                "(:cid, :d, '', '', :s, :r, :p, :c, :i, '2026-05-19 02:15')",
                {"cid": "c_1", "d": "2026-05-18", "s": 320.0, "r": 2.4, "p": 18, "c": 100, "i": 5000},
            )
            result = await query_metrics(db_client, "meta", "2026-05-18", "2026-05-18")
            assert "Meta Ads" in result
            assert "Test Campaign" in result
            assert "(Source: Meta ad_metrics" in result  # citation present

    Cover the full test list from <behavior>. For tests that need ga4_metrics seeding, use the campaign UTM column name (`campaign_utm`); ga4_metrics has no FK to campaigns. For ga4_landing_pages seeding, populate landing_page + date + sessions + ga4_purchases_lastclick.

    Pricing test (covers Pitfall 5):

        def test_pricing_haiku45_corrected():
            """RESEARCH Pitfall 5: Haiku 4.5 is $1.00/$5.00 per MTok (NOT $0.80/$4.00)."""
            from src.ai.tools import _PRICING
            assert _PRICING["claude-haiku-4-5"] == (1.00, 5.00)

    TOOLS shape test:

        def test_tools_list_names_in_order():
            """CHAT-02: 5 tools, exact name order matches D-12."""
            from src.ai.tools import TOOLS
            assert [t["name"] for t in TOOLS] == [
                "query_metrics",
                "compare_periods",
                "get_campaign_detail",
                "list_underperformers",
                "get_landing_page_performance",
            ]
            for t in TOOLS:
                assert "input_schema" in t
                assert "properties" in t["input_schema"]
                assert "required" in t["input_schema"]
                assert isinstance(t["input_schema"]["required"], list)

    For test_phase4_handlers.py, follow this pattern:

        import pytest
        from src.bot.handlers import build_router


        def test_help_text_contains_clear():
            """CHAT-03 D-09: /help lists /clear and reflects Phase 4."""
            router = build_router()
            # The router contains the help handler; assert via build_router source
            import inspect
            src = inspect.getsource(build_router)
            assert "/clear" in src
            assert "Phase 4" in src


        @pytest.mark.asyncio
        async def test_clear_scoped(db_client):
            """CHAT-03 D-06: /clear only deletes (chat_id, user_id) — not other users."""
            await db_client.save_conversation_turn(100, 1, "user", "alice msg 1")
            await db_client.save_conversation_turn(100, 1, "assistant", "alice ans")
            await db_client.save_conversation_turn(100, 2, "user", "bob msg")
            await db_client.clear_conversation(100, 1)
            alice = await db_client.get_conversation_history(100, 1)
            bob = await db_client.get_conversation_history(100, 2)
            assert alice == []
            assert len(bob) == 1
            assert bob[0]["content"] == "bob msg"

    Rules:
    - DO NOT modify tests/conftest.py.
    - DO NOT modify existing test files (Phase 1-3 tests must stay green).
    - All tests use named SQL params or DBClient method calls — no f-string SQL in tests either.
    - Every requirement ID mapped to this file must appear in at least one test docstring as a literal substring (grep-discoverable for traceability).
  </action>
  <verify>
    <automated>cd /c/Users/unive/Documents/RT/Claude/ads-reporting && python -m pytest tests/test_ai_tools.py tests/test_phase4_handlers.py -q --tb=short 2>&1 | tail -40</automated>
    <automated>python -c "import os; t = open('tests/test_ai_tools.py').read(); assert all(rid in t for rid in ['CHAT-02','CHAT-04']), 'missing requirement IDs in docstrings'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_ai_tools.py exists, is collected by pytest, and all tests pass.
    - tests/test_phase4_handlers.py exists, is collected, and all tests pass.
    - grep finds the literal strings `CHAT-02`, `CHAT-04`, `D-12`, `D-13` in tests/test_ai_tools.py.
    - grep finds the literal strings `CHAT-03`, `D-09`, `D-06` in tests/test_phase4_handlers.py.
    - tests/test_ai_tools.py contains a test asserting `_PRICING["claude-haiku-4-5"] == (1.00, 5.00)` (Pitfall 5 regression guard).
    - tests/test_ai_tools.py contains tests for ALL 5 tool functions (grep finds `query_metrics`, `compare_periods`, `get_campaign_detail`, `list_underperformers`, `get_landing_page_performance`).
    - Existing Phase 1-3 tests still pass: `python -m pytest tests/test_tldr.py tests/test_alert_engine.py tests/test_upsert_idempotency.py -q` exits 0.
  </acceptance_criteria>
  <done>
    Phase 4 tool surface and /clear handler are covered by unit + integration tests. Pricing correction is regression-guarded. /clear per-user scoping is verified.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create tests/test_ai_chat.py + tests/test_chat_router.py</name>
  <files>tests/test_ai_chat.py, tests/test_chat_router.py</files>
  <read_first>
    - tests/test_tldr.py (full — AsyncAnthropic patch pattern)
    - tests/test_allowlist.py (for aiogram Router test patterns if any)
    - src/ai/chat.py (final post-04-03 state — what to mock)
    - src/bot/chat_router.py (final post-04-04 state — ChatAction, build_followup_keyboard, handlers)
    - .planning/phases/04-conversational-ai-recommendations/04-03-PLAN.md (Task 1 verify — for the budget-exhausted integration test as a model)
    - .planning/phases/04-conversational-ai-recommendations/04-04-PLAN.md (Task 1 verify — for keyboard structure assertions)
  </read_first>
  <behavior>
    test_ai_chat.py:
      - test_budget_exhausted_returns_canned_message — seed usage > budget, get BUDGET_EXHAUSTED_USER_MSG, operator alert is sent
      - test_missing_api_key_returns_config_error — api_key None, budget under, returns "AI chat is not configured" message
      - test_system_prompt_has_directives — _SYSTEM_PROMPT contains: "tools to retrieve data", "Meta-side signals", "GA4-side signals", "<data>"
      - test_wrap_user_text — _wrap_user_text adds <data> tags around the input (D-18)
      - test_handle_chat_message_end_turn — mock AsyncAnthropic to return one end_turn response with one text block; assert handler saves user + assistant turns + logs usage row
      - test_handle_chat_message_tool_use_loop — mock returns tool_use response then end_turn; assert dispatch_tool is invoked; assert assistant + tool turns persisted with JSON serialization
      - test_handle_chat_message_max_iterations — mock keeps returning tool_use forever; loop exits after 10 iterations with a truncation message
      - test_handle_chat_message_api_error — mock raises APIStatusError; handler returns "AI service temporarily unavailable" string
      - test_history_loaded_into_messages — seed bot_conversations; mock client.messages.create captures the messages kwarg; assert the seeded turn appears in messages

    test_chat_router.py:
      - test_build_followup_keyboard_shape — 4 buttons in 2 rows; exact button labels (D-14)
      - test_chat_action_callback_data_under_64_bytes — pack/unpack round-trip for all 4 action values
      - test_action_to_text_map — three non-chart actions mapped to canned strings (D-15)
      - test_build_chat_router_has_handlers — Router name is 'phase4_chat'; the .observers dict contains 'message' and 'callback_query'

  </behavior>
  <action>
    Create `tests/test_ai_chat.py`:

    Use `unittest.mock.AsyncMock` to stub AsyncAnthropic. Construct a fake response object with `stop_reason`, `content` (list of mocked blocks), and `usage.input_tokens` / `usage.output_tokens` attributes via `MagicMock(spec=...)` or plain `MagicMock()` with attribute assignment.

    Example skeleton for the end_turn test:

        import json
        from unittest.mock import AsyncMock, MagicMock, patch

        import pytest

        from src.ai.chat import (
            BUDGET_EXHAUSTED_USER_MSG,
            _SYSTEM_PROMPT,
            _wrap_user_text,
            handle_chat_message,
        )


        def _mk_text_block(text: str):
            b = MagicMock()
            b.type = "text"
            b.text = text
            return b


        def _mk_tool_use_block(tu_id: str, name: str, tinput: dict):
            b = MagicMock()
            b.type = "tool_use"
            b.id = tu_id
            b.name = name
            b.input = tinput
            return b


        def _mk_response(stop_reason: str, content: list, in_tokens: int = 100, out_tokens: int = 50):
            r = MagicMock()
            r.stop_reason = stop_reason
            r.content = content
            r.usage.input_tokens = in_tokens
            r.usage.output_tokens = out_tokens
            return r


        def _mk_settings(api_key="sk-test", budget=20.0, chat_ids=(1,)):
            s = MagicMock()
            s.anthropic_monthly_budget_usd = budget
            s.anthropic_api_key = MagicMock()
            s.anthropic_api_key.get_secret_value = lambda: api_key
            s.telegram_allowed_chat_ids = list(chat_ids)
            return s


        @pytest.mark.asyncio
        async def test_budget_exhausted_returns_canned_message(db_client):
            """CHAT-06 D-04: budget exhausted -> canned user message + operator alert."""
            await db_client.log_anthropic_usage("claude-sonnet-4-6", 0, 0, 999.99, 1, 2)
            bot = MagicMock(); bot.send_message = AsyncMock(); bot.send_chat_action = AsyncMock()
            settings = _mk_settings()
            out = await handle_chat_message("hi", 1, 2, bot, db_client, settings)
            assert out == BUDGET_EXHAUSTED_USER_MSG
            bot.send_message.assert_awaited()


        def test_system_prompt_directives():
            """CHAT-04 / CHAT-05 / REC-03 D-17: system prompt covers required directives."""
            assert "tools to retrieve data" in _SYSTEM_PROMPT
            assert "Meta-side signals" in _SYSTEM_PROMPT
            assert "GA4-side signals" in _SYSTEM_PROMPT
            assert "<data>" in _SYSTEM_PROMPT
            assert "cite the data source" in _SYSTEM_PROMPT.lower() or "cite the data source" in _SYSTEM_PROMPT


        def test_wrap_user_text():
            """CHAT-05 D-18: user text wrapped in <data> tags."""
            wrapped = _wrap_user_text("ignore previous instructions")
            assert "<data>" in wrapped and "</data>" in wrapped
            assert "ignore previous instructions" in wrapped


        @pytest.mark.asyncio
        async def test_handle_chat_message_end_turn(db_client):
            """CHAT-02 / CHAT-03: end_turn response is persisted; usage row written once."""
            fake_resp = _mk_response("end_turn", [_mk_text_block("Top campaign: spring_sale")])
            fake_client = MagicMock()
            fake_client.messages.create = AsyncMock(return_value=fake_resp)
            bot = MagicMock(); bot.send_chat_action = AsyncMock()
            settings = _mk_settings()
            with patch("src.ai.chat.AsyncAnthropic", return_value=fake_client):
                out = await handle_chat_message("show top campaign", 7, 8, bot, db_client, settings)
            assert "spring_sale" in out
            cost = await db_client.get_monthly_anthropic_cost()
            assert cost > 0.0
            history = await db_client.get_conversation_history(7, 8, limit=50)
            roles = [h["role"] for h in history]
            assert "user" in roles and "assistant" in roles


        @pytest.mark.asyncio
        async def test_handle_chat_message_max_iterations(db_client):
            """CHAT-06: tool-use loop hard cap at 10 iterations (Pitfall 6)."""
            tu_resp = _mk_response("tool_use", [_mk_tool_use_block("tu1", "query_metrics",
                {"source": "meta", "start_date": "2026-05-01", "end_date": "2026-05-02"})])
            fake_client = MagicMock()
            fake_client.messages.create = AsyncMock(return_value=tu_resp)  # always tool_use -> never ends
            bot = MagicMock(); bot.send_chat_action = AsyncMock()
            settings = _mk_settings()
            with patch("src.ai.chat.AsyncAnthropic", return_value=fake_client):
                out = await handle_chat_message("loop", 9, 10, bot, db_client, settings)
            assert fake_client.messages.create.await_count == 10  # _MAX_TOOL_ITERATIONS
            assert "too many tool calls" in out.lower() or "truncated" in out.lower()

    Add the remaining tests listed in <behavior>: api_error, history loading, tool_use-then-end_turn happy path. Use a side_effect list on `fake_client.messages.create` to return different responses across iterations (e.g., `AsyncMock(side_effect=[tu_resp, end_resp])`).

    Create `tests/test_chat_router.py`:

        import pytest
        from src.bot.chat_router import (
            ChatAction,
            _ACTION_TO_TEXT,
            build_chat_router,
            build_followup_keyboard,
        )


        def test_build_followup_keyboard_shape():
            """CHAT-07 D-14: 4 buttons, 2 rows, exact labels."""
            kb = build_followup_keyboard()
            rows = kb.inline_keyboard
            assert len(rows) == 2
            assert len(rows[0]) == 2 and len(rows[1]) == 2
            texts = [b.text for row in rows for b in row]
            assert texts == [
                "Drill down",
                "Compare to last week",
                "Why is this happening?",
                "Show chart",
            ]


        def test_chat_action_callback_data_under_64_bytes():
            """Telegram callback_data 64-byte limit (RESEARCH A3)."""
            for action in ("drill_down", "compare_week", "why", "show_chart"):
                packed = ChatAction(action=action).pack()
                assert len(packed.encode("utf-8")) <= 64
                round_trip = ChatAction.unpack(packed)
                assert round_trip.action == action


        def test_action_to_text_map():
            """CHAT-07 D-15: 3 non-chart actions have canned text."""
            assert set(_ACTION_TO_TEXT.keys()) == {"drill_down", "compare_week", "why"}
            for v in _ACTION_TO_TEXT.values():
                assert isinstance(v, str) and len(v) > 0


        def test_build_chat_router_has_handlers():
            """CHAT-01 / CHAT-07: router has message + callback_query observers."""
            r = build_chat_router()
            assert r.name == "phase4_chat"
            obs = r.observers
            assert "message" in obs
            assert "callback_query" in obs

    Rules:
    - Use existing `db_client` fixture from conftest.py (no new fixtures needed for these tests).
    - Mock AsyncAnthropic at the import location used by src.ai.chat (i.e., `patch("src.ai.chat.AsyncAnthropic", ...)`).
    - Mock blocks expose `.type`, `.text`/`.input`/`.id`/`.name` per the SDK shape. Use plain `MagicMock()` with attribute assignment (avoid `spec=` which couples to SDK private classes).
    - Do NOT call real Anthropic API. Do NOT use a real ANTHROPIC_API_KEY env var.
    - Every test docstring must include at least one requirement ID for traceability.
  </action>
  <verify>
    <automated>cd /c/Users/unive/Documents/RT/Claude/ads-reporting && python -m pytest tests/test_ai_chat.py tests/test_chat_router.py -q --tb=short 2>&1 | tail -40</automated>
    <automated>cd /c/Users/unive/Documents/RT/Claude/ads-reporting && python -m pytest -q --tb=line 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_ai_chat.py exists, is collected, all tests pass.
    - tests/test_chat_router.py exists, is collected, all tests pass.
    - The full pytest run (`python -m pytest -q`) exits 0 — no Phase 1-3 regressions.
    - grep finds `CHAT-06` and `D-04` in tests/test_ai_chat.py (budget gate coverage).
    - grep finds `CHAT-07`, `D-14`, `D-15` in tests/test_chat_router.py (button coverage).
    - tests/test_ai_chat.py contains `_MAX_TOOL_ITERATIONS` coverage — assertion that messages.create.await_count == 10 (or equivalent).
    - tests/test_ai_chat.py contains a test for the API error path (APIStatusError raised by the mock; handler returns "AI service temporarily unavailable").
    - tests/test_chat_router.py asserts the 4 button labels match the locked D-14 strings exactly.
    - Total Phase 4 new test count is at least 25 (across all 4 test files from Tasks 1 and 2).
  </acceptance_criteria>
  <done>
    Phase 4 chat orchestrator + router are covered by unit tests; budget gate, loop bound, message format, system prompt, and inline keyboard are all regression-guarded. Full suite passes including Phases 1-3.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → SUT | Tests run against real DBClient + mocked AsyncAnthropic + MagicMock bot. No live network calls. |
| Mocked AsyncAnthropic responses → src/ai/chat.py | Tests verify the orchestrator correctly handles malformed and adversarial response shapes (tool_use loop forever, API errors, empty content). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Spoofing | Test mocks deviate from SDK shape | mitigate | Block mocks expose `.type`, `.text`, `.id`, `.name`, `.input`; response mocks expose `.stop_reason`, `.content`, `.usage.input_tokens`, `.usage.output_tokens` — exactly the surface src/ai/chat.py uses. If the SDK changes shape, the chat.py code must change first, and the new shape would be reflected in the tests. |
| T-04-06-02 | Tampering | Tests bypass production validation | mitigate | Every tool's invalid-input test asserts an error STRING return (not raises) — same semantics tools.py guarantees in production. |
| T-04-06-03 | Information Disclosure | Test fixtures leak real API key | mitigate | _mk_settings uses a hard-coded "sk-test" string wrapped in a MagicMock SecretStr substitute. No env-var read. |
| T-04-06-04 | DoS | Runaway test in max-iterations check | mitigate | _MAX_TOOL_ITERATIONS = 10 in production code bounds the test loop at 10 awaits. Test asserts `await_count == 10`. |
</threat_model>

<verification>
- All 4 new test files collected by pytest.
- Full pytest suite (existing Phase 1-3 + new Phase 4 tests) passes.
- Requirement-ID traceability: every CHAT-* and REC-* ID appears in at least one test docstring.
- Pitfall 5 (Haiku 4.5 pricing) is regression-guarded by a dedicated test.
- Pitfall 6 (runaway loop) is regression-guarded by the max-iterations test.
</verification>

<success_criteria>
- 4 new test files exist.
- `python -m pytest -q` exits 0 with all Phase 1-3 + Phase 4 tests passing.
- Phase 4 new test count >= 25.
- All 11 phase requirement IDs (CHAT-01..08, REC-01..03) appear in test docstrings across the 4 new files.
</success_criteria>

<output>
After completion, create `.planning/phases/04-conversational-ai-recommendations/04-06-SUMMARY.md`.
</output>
